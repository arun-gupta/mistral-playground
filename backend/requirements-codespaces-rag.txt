# FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Data validation and serialization
pydantic==2.5.0
pydantic-settings==2.1.0

# NumPy compatibility (pin to avoid PyTorch conflicts)
numpy<2.0.0

# Model inference (CPU-only)
transformers==4.37.0
torch==2.2.2
accelerate==0.25.0
ctransformers==0.2.27  # For GGUF model support
huggingface-hub>=0.19.3
sentence-transformers==2.2.2  # For text embeddings

# Document processing
pymupdf==1.23.8  # For PDF text extraction

# Vector storage (FAISS for Codespaces)
faiss-cpu==1.7.4  # CPU-only FAISS for vector similarity search

# Utilities
python-dotenv==1.0.0
httpx==0.27.0
urllib3==2.2.0
aiofiles==23.2.1
aiohttp==3.9.1  # For async HTTP requests in download service
tenacity==8.2.3

# Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1 